<!DOCTYPE html>
<html>
<head>
    <title>Todo list</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>

<div class="box" id="heading">
    <h1><%= kindOfDay%></h1>

</div>

<div class="box">
        <% for(let i =0; i < newListItem.length; i++){ %>
    <div class="item">
            <input type="checkbox">
            <p><%= newListItem[i] %> </p>
    </div>
        <% } %>
    <form action="/" method="post">
        <input type="text" name="newItem" placeholder="What Should I Do?" autocomplete="off">
        <button type="submit">+</button>
    </form>
</div>
</body>
</html>